[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "bouncer"
version = "0.1.0"
description = "Single-machine SQLite lease and ownership primitive"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.maturin]
python-source = "python"
module-name = "bouncer._bouncer_native"
