# Commits

No git commit has been created for this phase yet.

Local work recorded before commit:

- added `bouncer-extension` as a new workspace crate for the SQLite
  loadable extension
- added `attach_bouncer_functions` to `bouncer-honker` so SQL
  registration shares the same lease semantics as Rust callers
- added SQL-facing helpers for owner/token lookup in `bouncer-honker`
- added direct SQL-function tests in `bouncer-honker`
- added SQL/Rust interop tests in `packages/bouncer`
- kept `bouncer_bootstrap()` and `now_ms` explicit in the SQL contract
- documented the autocommit-only contract for SQL mutators
- added a core test pinning the nested-transaction failure mode
- added real extension-loading instructions to `bouncer-extension/README.md`
- applied the clippy cleanup in `to_sql_err`

Evidence reviewed locally:

- `cargo test`
  14 wrapper tests passed and 18 core tests passed
- `cargo clippy --workspace --all-targets -- -D warnings`
  passed cleanly
- `cargo build -p bouncer-extension`
  extension crate built successfully
- one-off bundled-SQLite Rust probe loaded
  `target/debug/libbouncer_ext.dylib` and successfully executed:
  - `bouncer_bootstrap()`
  - `bouncer_claim(...)`
  - `bouncer_owner(...)`
  - `bouncer_token(...)`
