# Commits

Phase: 012 — SQLite behavior matrix

## Commits

- No landing commit has been created for this phase yet.

## Evidence

- `cargo test -p bouncer-core --test sqlite_matrix`
  Result: 15 matrix tests pass.
- `cargo test -p bouncer --test sqlite_matrix`
  Result: 5 wrapper matrix tests pass.
- `make test-rust`
  Result: full Rust suite passes, including core, wrapper,
  extension-load, invariant, and matrix tests.
- `make test`
  Result: full Rust suite, extension build, editable Python build, and
  20 Python tests pass.

## Notes

- Local closeout state before commit:
  - added file-backed SQLite matrix coverage in
    `bouncer-core/tests/sqlite_matrix.rs`
  - added wrapper matrix coverage in
    `packages/bouncer/tests/sqlite_matrix.rs`
  - changed `to_sql_err` so SQL functions preserve underlying SQLite
    `BUSY` / `LOCKED` errors where the scalar-function boundary allows it
  - merged landed Phase 012 behavior into `SYSTEM.md`, `ROADMAP.md`, and
    `CHANGELOG.md`
