# Commits

- `f72da21` `Make Bouncer SQL mutators transactional`

Local work recorded before commit:

- extracted `pub(crate)` `claim_in_tx`, `renew_in_tx`, and
  `release_in_tx` helpers in `bouncer-honker`
- kept the public Rust helpers as thin `BEGIN IMMEDIATE` wrappers
- changed `attach_bouncer_functions` to branch on
  `conn.is_autocommit()`
- made `bouncer_claim`, `bouncer_renew`, and `bouncer_release` work
  inside caller-owned explicit transactions and savepoints
- replaced the old nested-transaction failure test with explicit
  transaction, multi-mutator, read-in-transaction, semantic-stress,
  and savepoint tests
- documented the `*_in_tx` precondition directly on the helper
  definitions
- updated the extension and repo docs to describe the transactional SQL
  contract
- merged the landed Phase 004 behavior into `SYSTEM.md`, `CHANGELOG.md`,
  and `ROADMAP.md`

Evidence reviewed locally:

- `cargo test`
  14 wrapper tests passed and 24 core tests passed
- `cargo clippy --workspace --all-targets -- -D warnings`
  passed cleanly
- `cargo build -p bouncer-extension`
  extension crate built successfully
