# Commits

- `5aaf544` — `Rename Bouncer core crate`

Verification:

- `cargo fmt --all`
- `cargo test -p bouncer-core -p bouncer`
  - `bouncer`: 35 tests passed
  - `bouncer-core`: 26 tests passed
- `cargo build -p bouncer-extension`
- `cargo clippy --workspace --all-targets -- -D warnings`
