[project]
name = "bouncer-workspace"
version = "0.1.0"
description = "bouncer development workspace"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "maturin>=1.7,<2",
    "pytest>=8.3",
]

[tool.pytest.ini_options]
testpaths = ["packages/bouncer-py/tests"]
